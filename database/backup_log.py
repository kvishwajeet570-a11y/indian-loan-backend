
from database.db import get_db from datetime import datetime     def get_backup_collection():     from database.db import get_db     db = get_db()     db = get_db()     return db["backup_logs"]    def save_backup_log(filename):      backup_collection.insert_one({          "file": filename,          "date": datetime.utcnow(),          "status": "success"      })   def get_backup_logs():      return list(          backup_collection.find(              {},              {"_id": 0}          )      )
