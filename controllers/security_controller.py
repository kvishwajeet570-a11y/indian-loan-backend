
from database.db import get_db from datetime import datetime from flask_jwt_extended import get_jwt_identity from services.cleanup_service import run_cleanup      def get_security_collection():     from database.db import get_db     db = get_db()     db = get_db()     return db["security"]   def get_blocked_collection():     from database.db import get_db     db = get_db()     db = get_db()     return db["blocked_users"]    # Block User  def block_user_controller(data):      email = data["email"]      blocked_collection.update_one(          {"email": email},          {              "$set": {                  "email": email,                 "blocked": True,                 "date": datetime.utcnow()              }          },          upsert=True      )      return {          "status": True,         "message": "User Blocked Successfully"      }   # Unblock User  def unblock_user_controller(data):      email = data["email"]      blocked_collection.delete_one(          {"email": email}      )      return {          "status": True,         "message": "User Unblocked"      }   # Check Block  def check_block_controller(email):      user = blocked_collection.find_one(          {"email": email}      )      if user:          return True      return False   # Log Security Event  def log_security_event(event, ip, device):      security_collection.insert_one({          "event": event,         "ip": ip,         "device": device,         "date": datetime.utcnow()      })      return True   # Get Security Logs  def get_logs_controller():      logs = list(          security_collection.find(              {},              {"_id": 0}          )      )      return {          "status": True,         "data": logs      }   def cleanup_controller():      return run_cleanup()
